# Tasks: restart-resilience-verify

## 1. Restart gate fixture (design D1, D3, D4)

- [x] 1.1 Add a session-scoped autouse fixture to `molecule/default/tests/conftest.py` that builds Host objects from `MOLECULE_INVENTORY_FILE` via AnsibleRunner (host list = master + node1 FQDNs, overridable via comma-separated `RESTART_HOSTS`; no-op when the env var is absent), issues `systemctl restart kubelet` on all hosts simultaneously, then polls until each host's `kubelet` reports active (running) AND both nodes report Ready in `kubectl get nodes` (~5 min budget), asserting with an error naming any host that did not recover — verify by running pytest outside molecule (everything skipped as before) and under molecule (log shows kubelet restarted on both hosts and the suite starts only after readiness; under-molecule half verified via task 2.1's e2e run)
- [x] 1.2 Verify gate failure semantics: run pytest under molecule with `RESTART_HOSTS=does-not-exist.osgiliath.test` and confirm verify fails with an error naming that host and no functional tests execute (verified: "Restart gate: host(s) does-not-exist.osgiliath.test ... not found in inventory", 32 setup errors, 0 tests ran)

## 2. End-to-end verification

- [x] 2.1 Run `tox -e converge-monorepo --scenario-name=parallels` then `tox -e verify-monorepo --scenario-name=parallels`: gate restarts kubelet on both hosts and the full suite passes against the post-restart state — verify by log evidence of the restart + readiness wait and a green pytest run (verified: create+converge clean — master ok=270/node1 ok=149 failed=0; verify 32/32 passed; gate log lines "Restart gate: restarting kubelet on master.osgiliath.test, node1.osgiliath.test ..." → "...kubelet active and all nodes Ready..."; kubelet ActiveEnterTimestamp advanced from converge's 14:43:08 CDT to 14:46:35 CDT during verify). Full `molecule test` (test-exec-monorepo) initially failed at verify — root cause: converge ends while node1 is still NotReady (join only waits for the node object to appear; calico-node deployment lags ~4-9 min), so the gate restarted kubelet before CNI was ready and the 300s budget expired. Fixed by adding a final "Wait for all nodes to be Ready" play (600s budget) to `molecule/parallels/converge.yml` that runs after both converge plays (per-play handler flushes restart containerd/kubelet at each play's end, so the wait must come after them); on a settled cluster restart recovery is <30s, well within the gate budget
- [x] 2.2 Confirm the no-op path: run pytest directly without `MOLECULE_INVENTORY_FILE` set and confirm all tests skip with no kubelet restart attempted (verified: 32 skipped, clean exit)
