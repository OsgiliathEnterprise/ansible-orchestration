---

- name: Destroy-kubelet-volumes | get volume name
  ansible.builtin.find:
    paths: "{{ pod_files }}/volumes"
    file_type: any
    recurse: false
  register: kube_volume
  become: Yes

- name: Destroy-kubelet-volumes | get volumes name
  ansible.builtin.debug:
    msg: "{{ kube_volume }}"
    verbosity: 3

- name: Destroy-kubelet-volumes | unmount
  ansible.posix.mount:
    path: "{{ mounted_volume }}"
    state: absent
  loop: "{{ kube_volume.files | default([]) | map(attribute='path') | list }}" # avoids ansible proxying string when array is one
  loop_control:
    loop_var: mounted_volume
  when: kube_volume.files | length >= 1
  become: Yes
